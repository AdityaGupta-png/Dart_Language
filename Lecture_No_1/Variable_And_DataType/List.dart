void main() {
  // List --> it used to store the data of specific data type
  // List having the specific type
  // If i want only the Name type of list the i use the List<String>

  List<String> names = ["Aditya", "Ramesh", "Navnath", "Chandan"];
  print("All names of the list is ${names}");
  print("First name of the list is ${names[0]}");

  // Find the total length of the string
  print("The total length of the string is ${names.length}");

  // here i ame the some type of the list for the practice
  // 1 --> INTEGER LIST
  // List<int> numbers = [1, 2, 3, 4, 5, 66, 77, 88, 9, 9];

  // // 2 --> boolean type of the list
  // List<bool> store = [true, false];

  // Sets and list are same but 
  //  list --> we use the Square bracket 
  //  set --> we use the curly braces 
}
